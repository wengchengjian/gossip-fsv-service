apply plugin: 'java'

group = 'com.wcj.fsv.starter'
version = "0.0.1.SNAPSHOT"
description = 'gossip-fsv-starter'

dependencies {
    implementation project(":gossip-fsv-max")
}
jar.enabled = false
/* 需要org.springframework.boot插件 不需要打包成可执行的jar*/
bootJar.enabled = true